<%= form_for(@avaliacao) do |f| %>
  <% if @avaliacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@avaliacao.errors.count, "error") %> prohibited this avaliacao from being saved:</h2>

      <ul>
      <% @avaliacao.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= link_to 'Antropometria', new_antropo_path %>
  <%= link_to 'IMC', new_calculo_imc_pathh %>



    <div class="actions">
    <%= f.submit "Salvar"%>
  </div>
<% end %>
